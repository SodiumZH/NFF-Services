package net.sodiumstudio.nautils.annotation;

/**
 * Labels that a method or class should only exist or be called on server (either single-player server thread or dedicated server).
 */
public @interface ServerOnly
{

}
